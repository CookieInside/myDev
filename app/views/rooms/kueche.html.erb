<h1>Luftdaten für die Küche</h1>
<%= line_chart Measuring.where(room: "kueche").group_by_minute(:created_at, range: 0.5.day.ago..Time.now).sum(:temperature), label: "Lufttemperatur", points: false, suffix: "°C", decimal: ",", refresh: 60 %>
<%= line_chart Measuring.where(room: "kueche").group_by_minute(:created_at, range: 0.5.day.ago..Time.now).sum(:humidity), label: "Luftfeuchtigkeit", points: false, suffix: "%", decimal: ",", refresh: 60 %>
